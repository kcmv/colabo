/*
 * Public API Surface of `@colabo-flow/b-go`
 */

/*
import { CfGoDbSchema } from './lib/go-db-schema';
export { CfGoDbSchema };

export { ColaboFlowGoApi } from './lib/go-api';
export { ColaboFlowGoDb, MainTypes, ActionTypes, SearchParams } from './lib/go-db';

export { GoDbVo } from './lib/go-db-vo';
*/

// setting express route <start>
// it gets all HTTP verb methods (index, post, ...) available

/*

import * as ColaboFlowGoAPI_All from './lib/go-api';

const API_ROUTE = 'colabo-flow/go';

export function initialize(app) {
    console.log("[puzzle(colabo-flow/go) - /index.js] Registering ColaboFlow Go API to: %s", API_ROUTE);

    var colaboFlowGo = app.resource(API_ROUTE, ColaboFlowGoAPI_All, { id: 'type?/:actionType?/:searchParam?' });
}
*/
// setting express route <end>
