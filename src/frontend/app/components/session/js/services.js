/**
* the namespace for the notify part of the KnAllEdge system
* @namespace knalledge.session
*/

(function () { // This prevents problems when concatenating scripts that aren't strict.
'use strict';
//this function is strict...

var sessionServices = angular.module('sessionServices', ['ngResource', 'Config']);

/**
* the namespace for core services for the Gardening system
* @namespace knalledge.session.sessionServices
*/

/**
* Service that is a plugin into knalledge.MapVisualization
* @class ApprovalNodeService
* @memberof knalledge.session.sessionServices
*/

}()); // end of 'use strict';
